<nav class="navbar navbar-default">
  <div class="container">
  <div class="row">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Pins <span class="glyphicon glyphicon-pushpin" aria-hidden="true"></span></a>
      <p class="navbar-text">Disqussion platform</p>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% Category.all.each do |category| %>
          <li><%= link_to category.name, posts_path(:category_id => category.id) %></li>
        <% end %>
      </ul>
      <%= form_tag(posts_path, :method => "get", class: "navbar-form navbar-left") do %>
        <div class="form-group">
          <%= text_field_tag :search, params[:search], placeholder: "Search Pins", class: "form-control" %>
        </div>
        <%= submit_tag "Search", :name => nil, class: "btn btn-default" %>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li><%= link_to raw('<span class="glyphicon glyphicon-plus" aria-hidden="true"></span> New Post'), new_post_path %></li>
        <li><%= link_to raw('<span class="glyphicon glyphicon-off" aria-hidden="true"></span> Sign out'), destroy_user_session_path, method: :delete %></li>
      <% else %>
        <li><%= link_to raw('<span class="glyphicon glyphicon-user" aria-hidden="true"></span> Sign in'), new_user_session_path %></li>
        <li><%= link_to raw('<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Sign up'), new_user_registration_path %></li>
      <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
  </div>
  </div>
</nav>